/**
 * Created by ajiteshkumar on 17/5/17.
 */

describe('mlDBController Module', () => {
  beforeEach(() => {

  });
  afterEach(() => {

  });
  it('should create an entry in User collection for collectionName equal to "users" by using Accounts package', () => {
  });
  it('should create an entry in any other collection using generic method', () => {
  });
  it('should create an audit entry for creation of new entry in any collections that support audit', () => {
  });
  it('should update an audit entry for upsert and multi options', () => {
  });
})
